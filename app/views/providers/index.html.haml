%h1 All Providers
= render 'filtering_form'
%table.tablesorter
  %thead
    %th Name
    %th City
    %th Insurers
  %tbody
    - @providers.each do |provider|
      %tr
        %td= link_to provider.name, provider_path(provider.id)
        %td= provider.city
        %td= provider.insurers.map { |ins| ins.name } .join(", ")

= link_to providers_path do
  %button Clear Search